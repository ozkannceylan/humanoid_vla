# Dependency management moved to pyproject.toml — install the package instead:
#
#   pip3 install -e .[dev,track]          # core + tests/lint + wandb
#   pip3 install -e .[lang]               # + transformers (CLIP text conditioning)
#
# For CUDA-specific torch builds, install torch first from the matching index:
#   pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# This file is kept as a thin alias for tooling that expects it.
-e .
